{
  "name": "cinderblock",
  "version": "0.0.1",
  "description": "A new programming language",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test ./dist/**/*.test.js",
    "package": "npm run build && npm run test && pkg .",
    "test:hello-world": "npm run build && cd ../cinderblock-tests/hello-world && cinderblock compile",
    "test:chained-operators": "npm run build && cd ../cinderblock-tests/chained-operators && cinderblock compile",
    "test:if-expressions": "npm run build && cd ../cinderblock-tests/if-expressions && cinderblock compile",
    "test:embedded-c": "npm run build && cd ../cinderblock-tests/embedded-c && cinderblock compile",
    "test:function-overloading": "npm run build && cd ../cinderblock-tests/function-overloading && cinderblock compile",
    "test:lambdas": "npm run build && cd ../cinderblock-tests/lambdas && cinderblock compile",
    "test:partial-invokation": "npm run build && cd ../cinderblock-tests/partial-invokation && cinderblock compile",
    "test:enums": "npm run build && cd ../cinderblock-tests/enums && cinderblock compile",
    "test:effuse": "npm run build && cd ../effuse-cinderblock && cinderblock workspace compile"
  },
  "private": true,
  "bin": {
    "cinderblock": "./bin/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cinderblock-lang/cinderblock-compiler.git"
  },
  "author": "Paul Taylor",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cinderblock-lang/cinderblock-compiler/issues"
  },
  "homepage": "https://github.com/cinderblock-lang/cinderblock-compiler#readme",
  "devDependencies": {
    "@types/node": "^20.8.10",
    "pkg": "^5.8.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "yargs": "^17.7.2",
    "zod": "^3.22.4"
  },
  "pkg": {
    "targets": [
      "node18-linux-x64"
    ],
    "outputPath": "pkg"
  }
}
